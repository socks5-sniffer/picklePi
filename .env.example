# GEMINI_API_KEY: Required by the generate-level.ts developer script.
# This script uses the Gemini API to generate new curriculum levels.
# Obtain a key from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Note: Do not commit actual secrets to version control.
# This file is safe to commit — it contains only placeholder values.
# Copy it to .env and fill in your real values:
#   cp .env.example .env