# Firebase service account key path (do NOT commit the actual .json file)
FIREBASE_CREDENTIALS=backend/firebase-secret.json

# Flask settings
FLASK_ENV=development
FLASK_PORT=5000

# CORS: comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:5173,https://localhost:5173
